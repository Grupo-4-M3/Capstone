import { StyledSection } from "./styles";

function Card({ pessoa }) {
  return (
    <StyledSection>
      <figure>
        <img src="" alt="" />
      </figure>
    </StyledSection>
  );
}

export default Card;
